from setuptools import find_packages
from setuptools import setup

setup(
    name='Pre-commit Pytest Setup File',
    version='1.0',
    packages=find_packages(),
    install_requires=['pytest']
    )
